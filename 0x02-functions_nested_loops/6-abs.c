#include "main.h"
/**
 * int _abs - computes the absolute value of an integer.
 * @i: the integer to be converted
 * Return: absoulte value of integer
 */
int _abs(int i)
{
	if (i < 0)
		i = i * -1;
	return (i);
}
